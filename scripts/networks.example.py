# Copy to networks.py and fill in
NETWORK = "http://localhost:8545"  # Ethereum mainnet RPC
ETHERSCAN_API_KEY = ""

# RPCs for --network; "ethereum" is the default
NETWORKS = {
    "ethereum": NETWORK,
    "robinhood": "",
}
